{
    "dependencies": {
        "dotenv": "^16.4.5",
        "grammy": "^1.30.0",
        "node-telegram-bot-api": "^0.66.0",
        "ts-node": "^10.9.2",
        "typescript": "^5.6.2",
        "web3": "^4.13.0"
    },
    "scripts": {
        "index": "node --inspect --require ts-node/register/transpile-only index.ts",
        "pastEvents": "node --inspect --require ts-node/register/transpile-only pastEvents.ts",
        "websocket": "node --inspect --require ts-node/register/transpile-only websocket.ts"
    },
    "devDependencies": {
        "@types/node-telegram-bot-api": "^0.64.7"
    }
}
